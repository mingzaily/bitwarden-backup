#### 模块 E：前端列表层（2 任务点）

**文件**: `/Users/mingzaily/Code/tool/bitwarden-backup/web/frontend/src/views/Tasks.vue`

- [ ] **任务 E.1**：任务列表显示类型标签（1 点）
  - **输入**：当前任务列表布局（第 40-51 行）
  - **输出**：显示任务类型徽章（定时/手动）
  - **关键步骤**：
    1. 在任务名称旁边添加类型徽章
    2. 根据 `task.cron_expression` 是否为空判断类型
    3. 定时任务显示蓝色徽章："定时"
    4. 手动任务显示灰色徽章："手动"

- [ ] **任务 E.2**：Cron 表达式条件显示（1 点）
  - **输入**：当前 Cron 表达式显示逻辑（第 46-50 行）
  - **输出**：只在有 Cron 表达式时显示
  - **关键步骤**：
    1. 添加 `v-if="task.cron_expression"` 条件
    2. 手动任务显示提示文字："手动触发"
    3. 保持图标和样式一致

**验收标准**：
- 任务列表清晰显示任务类型（定时/手动）
- 手动任务不显示 Cron 表达式
- 定时任务正常显示 Cron 表达式
- 徽章样式与现有设计一致

---

#### 模块 F：术语统一（2 任务点）

**文件**: 多个前端文件

- [ ] **任务 F.1**：前端页面和组件术语统一（2 点）
  - **输入**：所有包含"定时任务"的前端文件
  - **输出**：统一改为"备份任务"
  - **关键步骤**：
    1. `web/frontend/src/views/Tasks.vue`：页面标题 "备份任务" → "备份任务"（已正确）
    2. `web/frontend/src/components/TaskModal.vue`：模态框标题 "新建任务" / "编辑任务" → "新建备份任务" / "编辑备份任务"
    3. `web/frontend/src/router/index.js`：路由名称和标题检查
    4. 全局搜索 "定时任务" 并替换为 "备份任务"

**验收标准**：
- 所有用户可见的文字统一为"备份任务"
- 路由和导航标签一致
- 无遗漏的"定时任务"术语

---
